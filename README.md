# Venkata Reddy — Portfolio

Simple static portfolio with `index.html`, `styles.css`, and `script.js`.

Open `index.html` in a browser to view locally.

To push to GitHub:
- Create a repository on GitHub (or provide a remote URL).
- Run `git remote add origin <REMOTE_URL>` and `git push -u origin main`.

If you want, I can push it for you — paste a repository URL or allow creation via `gh`.